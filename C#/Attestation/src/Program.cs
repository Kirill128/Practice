using System;
using System.IO;
using System.Collections.Generic;

namespace Attestation
{
    class Program
    {
        static void Main(string[] args)
        {
            Console.WriteLine("Input abit:");
            AbiturientList abits=new AbiturientList(5);
            foreach(Abiturient a in AbiturientList.abiturWithBadNotes(abits)){
                
            }
            foreach(Abiturient a in AbiturientList.abiturWithNotesNoLessThenInput(abits,6)){
                
            }
            N=5;
            for(int i=0;i<5;i++){
                
            }
        }
    }
}
